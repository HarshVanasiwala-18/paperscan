services:
  - type: web
    name: paperscan
    runtime: docker
    plan: starter
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PAPERSCAN_SKIP_OCR
        value: "0"
    healthCheckPath: /
